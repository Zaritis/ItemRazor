@page "{handler?}"
@model ItemRazor.Pages.Item.GelAllItemsAsCardsModel
@{
}
<div class="container-fluid pt-3">
<h3 class="font-weight-light text-white">Kanban Board</h3>
<div class="small  text-light">Drag and drop between swim lanes</div>

<div class="row flex-row flex-sm-nowrap py-3">
    <div class="col-3">
        <div class="card bg-light">
            <div class="card-body">
                <h6 class="card-title text-uppercase text-truncate py-2">To Do</h6>
                <div class="items border border-light">
                    @if (@Model.Items != null)
                    {
                        foreach (var item in Model.Items)
                        {
                            <div class="card-body p-2">
                                <div class="card-title">
                                    @item.Name
                                </div>
                                <p>Navn: @item.Name Pris: @item.Price</p>
                            </div>

                        }
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="col-3">
        <div class="card bg-light">
            <div class="card-body">
                <h6 class="card-title text-uppercase text-truncate py-2">Doing</h6>
                <div class="items border border-light">
                    @if (@Model.Items != null)
                    {
                        foreach (var item in Model.Items)
                        {
                            <div class="card-body p-2">
                                <div class="card-title">
                                    @item.Name
                                </div>
                                <p>Navn: @item.Name Pris: @item.Price</p>
                            </div>

                        }
                    }
                </div>
            </div>
        </div>
    </div>
    <div class="col-3">
        <div class="card bg-light">
            <div class="card-body">
                <h6 class="card-title text-uppercase text-truncate py-2">Done</h6>
                <div class="items border border-light">
                    @if (@Model.Items != null)
                    {
                        foreach (var item in Model.Items)
                        {
                            <div class="card-body p-2">
                                <div class="card-title">
                                    @item.Name
                                </div>
                                <p>Navn: @item.Name Pris: @item.Price</p>
                            </div>

                        }
                    }
                </div>
            </div>
        </div>
    </div>
    <div class="col-3">
        <div class="card bg-light">
            <div class="card-body">
                <h6 class="card-title text-uppercase text-truncate py-2">Done Done</h6>
                <div class="items border border-light">
                    @if (@Model.Items != null)
                    {
                        foreach (var item in Model.Items)
                        {
                            <div class="card-body p-2">
                                <div class="card-title">
                                    @item.Name
                                </div>
                                <p>Navn: @item.Name Pris: @item.Price</p>
                            </div>

                        }
                    }
                </div>
            </div>
        </div>
    </div>
</div>
</div>